export type loginCredsType = {
    email: string;
    password: string;
};

export type registerCredsType = {
    username: string;
    email: string;
    password: string;
    confirmPassword: string
};